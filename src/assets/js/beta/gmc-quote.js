const toggleRatioQuote = (val) => {
    if (val == '1') {
        $('.section_renewal').show();
    } else {
        $('.section_renewal').hide();
    }
}

const gmcQuoteRatio = (ratio) => {
    // if (val == '1') {
    //     $('.section_renewal').show();
    // } else {
    //     $('.section_renewal').hide();
    // }
    alert(ratio);
}